import 'package:flutter/widgets.dart';

abstract class Dimense {
  static SizedBox boxWidth(double width) => SizedBox(width: width);
  static SizedBox boxHeight(double height) => SizedBox(height: height);
}
